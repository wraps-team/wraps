{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": [
    "DATABASE_URL",
    "BETTER_AUTH_SECRET",
    "PASSKEY_RP_ID",
    "PASSKEY_RP_NAME",
    "NEXT_PUBLIC_APP_URL",
    "CORS_ORIGIN",
    "AWS_ACCESS_KEY_ID",
    "AWS_SECRET_ACCESS_KEY",
    "AWS_BACKEND_ACCOUNT_ID",
    "AWS_PROFILE",
    "AWS_ROLE_ARN",
    "AWS_REGION",
    "WRAPS_EMAIL_ROLE_ARN",
    "EMAIL_FROM",
    "BLOB_READ_WRITE_TOKEN"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "env": [
        "DATABASE_URL",
        "BETTER_AUTH_SECRET",
        "PASSKEY_RP_ID",
        "PASSKEY_RP_NAME",
        "NEXT_PUBLIC_APP_URL",
        "CORS_ORIGIN",
        "AWS_ACCESS_KEY_ID",
        "AWS_SECRET_ACCESS_KEY",
        "AWS_BACKEND_ACCOUNT_ID",
        "AWS_ROLE_ARN",
        "AWS_REGION",
        "WRAPS_EMAIL_ROLE_ARN",
        "EMAIL_FROM",
        "BLOB_READ_WRITE_TOKEN"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "cache": false
    },
    "test:coverage": {
      "cache": false,
      "outputs": ["coverage/**"]
    },
    "lint": {
      "cache": false
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "clean": {
      "cache": false
    },
    "//#check": {},
    "//#fix": {
      "cache": false
    }
  }
}
