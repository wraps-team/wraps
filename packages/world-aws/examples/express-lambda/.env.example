WORKFLOW_TARGET_WORLD=@wraps.dev/world-aws
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=123456789012
WORKFLOW_AWS_TABLE_PREFIX=workflow
WORKFLOW_AWS_QUEUE_PREFIX=workflow

# Optional: enable at-rest encryption for workflow data (AES-256-GCM)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# WORKFLOW_AWS_ENCRYPTION_KEY=
